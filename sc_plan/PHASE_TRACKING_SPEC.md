# SuperClaude Phase Tracking - 기술 명세서

Phase Tracking 기능의 아키텍처, 데이터 스키마, 알고리즘 및 구현 상세입니다.

---

## 목차

1. [아키텍처 개요](#1-아키텍처-개요)
2. [데이터 스키마](#2-데이터-스키마)
3. [마크다운 파싱 규칙](#3-마크다운-파싱-규칙)
4. [상태 머신](#4-상태-머신)
5. [의존성 관리](#5-의존성-관리)
6. [Serena Memory 연동](#6-serena-memory-연동)
7. [워크플로우 상세](#7-워크플로우-상세)
8. [에러 처리](#8-에러-처리)
9. [구현 파일 구조](#9-구현-파일-구조)
10. [확장 고려사항](#10-확장-고려사항)

---

## 1. 아키텍처 개요

### 1.1 시스템 구성도

```
┌─────────────────────────────────────────────────────────────┐
│                    SuperClaude Framework                     │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  ┌───────────────┐     ┌───────────────┐                   │
│  │ /sc:plan-*    │────▶│  Plan Router  │                   │
│  │   commands    │     └───────┬───────┘                   │
│  └───────────────┘             │                           │
│                                ▼                           │
│  ┌─────────────────────────────────────────────────────┐   │
│  │                  Plan Engine                         │   │
│  │  ┌──────────┐ ┌──────────┐ ┌──────────┐            │   │
│  │  │ Parser   │ │ State    │ │ Deps     │            │   │
│  │  │ Module   │ │ Manager  │ │ Resolver │            │   │
│  │  └──────────┘ └──────────┘ └──────────┘            │   │
│  └─────────────────────────────────────────────────────┘   │
│                                │                           │
│            ┌───────────────────┼───────────────────┐       │
│            ▼                   ▼                   ▼       │
│  ┌──────────────┐    ┌──────────────┐    ┌──────────────┐ │
│  │  Markdown    │    │  Progress    │    │   Serena     │ │
│  │  Source      │    │  YAML        │    │   Memory     │ │
│  │  (.md)       │    │  (.yaml)     │    │              │ │
│  └──────────────┘    └──────────────┘    └──────────────┘ │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

### 1.2 컴포넌트 역할

| 컴포넌트 | 역할 |
|----------|------|
| **Plan Router** | 명령어 파싱 및 적절한 핸들러로 라우팅 |
| **Parser Module** | 마크다운 계획서를 구조화된 데이터로 변환 |
| **State Manager** | Phase/Task 상태 변경 및 영속화 |
| **Deps Resolver** | 의존성 그래프 구축 및 검증 |
| **Progress YAML** | 로컬 상태 저장소 |
| **Serena Memory** | 세션 간 상태 영속화 |

### 1.3 데이터 흐름

```
1. 계획 로드 플로우
   .md 파일 → Parser → 구조화 데이터 → State Manager → .yaml + Memory

2. 상태 변경 플로우
   명령어 → Deps Resolver (검증) → State Manager → .yaml + Memory 동기화

3. 조회 플로우
   명령어 → State Manager → .yaml 읽기 → 포맷팅 → 출력

참고: 모든 명령어는 /sc:plan-<action> 형식 사용 (예: /sc:plan-load, /sc:plan-start)
```

---

## 2. 데이터 스키마

### 2.1 Progress YAML 스키마

**파일 위치**: `.superclaud/progress.yaml`

```yaml
# 스키마 버전
version: "1.0"

# 활성 계획 (한 번에 하나만)
active_plan: "Design_Phase1"

# 등록된 계획 목록
plans:
  Design_Phase1:
    # 메타데이터
    source: "docs/Design_Phase1.md"        # 원본 파일 경로
    source_hash: "sha256:abc123..."        # 원본 파일 해시 (변경 감지)
    registered_at: "2025-01-15T10:00:00Z"  # 등록 시각
    revision: 1                             # 재설계 횟수

    # Phase 목록
    phases:
      - id: "Phase 1-1"                    # Phase 식별자
        name: "기반 시스템"                  # Phase 이름
        status: "completed"                 # 상태
        depends_on: []                      # 의존 Phase ID 목록
        started_at: "2025-01-15T10:30:00Z" # 시작 시각
        completed_at: "2025-01-15T14:00:00Z" # 완료 시각

        # 태스크 목록
        tasks:
          - name: "프로젝트 설정"
            status: "completed"
          - name: "GameManager 싱글톤"
            status: "completed"
          - name: "SafeAreaManager"
            status: "completed"
          - name: "기본 씬 구조"
            status: "completed"

      - id: "Phase 1-2"
        name: "플레이어 시스템"
        status: "in_progress"
        depends_on: ["Phase 1-1"]
        started_at: "2025-01-16T09:00:00Z"
        completed_at: null

        tasks:
          - name: "Player 씬/스크립트"
            status: "completed"
          - name: "터치 입력 (드래그 이동)"
            status: "in_progress"
          - name: "자동 사격 시스템"
            status: "pending"
          - name: "에너지 흡수 메커니즘"
            status: "pending"
          - name: "반격 시스템"
            status: "pending"

      - id: "Phase 1-3"
        name: "적/탄환 시스템"
        status: "pending"
        depends_on: ["Phase 1-2"]
        started_at: null
        completed_at: null
        tasks: [...]

    # 변경 이력
    history:
      - action: "registered"
        date: "2025-01-15T10:00:00Z"
        details: null

      - action: "phase_started"
        date: "2025-01-15T10:30:00Z"
        details:
          phase: "Phase 1-1"

      - action: "phase_completed"
        date: "2025-01-15T14:00:00Z"
        details:
          phase: "Phase 1-1"
          duration_hours: 3.5

      - action: "redesign"
        date: "2025-01-18T14:00:00Z"
        details:
          phase: "Phase 1-2"
          reason: "터치 드래그 → 가상 조이스틱 변경"
          affected_phases: ["Phase 1-2", "Phase 1-3"]
          revision: 2
```

### 2.2 스키마 타입 정의

```typescript
// TypeScript 스타일 타입 정의 (구현 참고용)

interface ProgressFile {
  version: string;
  active_plan: string | null;
  plans: Record<string, Plan>;
}

interface Plan {
  source: string;
  source_hash: string;
  registered_at: string;  // ISO 8601
  revision: number;
  phases: Phase[];
  history: HistoryEntry[];
}

interface Phase {
  id: string;
  name: string;
  status: PhaseStatus;
  depends_on: string[];
  started_at: string | null;
  completed_at: string | null;
  tasks: Task[];
}

type PhaseStatus = "pending" | "in_progress" | "completed" | "blocked";

interface Task {
  name: string;
  status: TaskStatus;
}

type TaskStatus = "pending" | "in_progress" | "completed";

interface HistoryEntry {
  action: HistoryAction;
  date: string;
  details: Record<string, any> | null;
}

type HistoryAction =
  | "registered"
  | "phase_started"
  | "phase_completed"
  | "phase_paused"
  | "phase_rollback"
  | "redesign"
  | "task_completed";
```

---

## 3. 마크다운 파싱 규칙

### 3.1 지원 형식

```markdown
## N. 구현 순서                    ← 섹션 헤더 (진입점)
또는
## Implementation Order
또는
## 구현 계획

### Phase X-Y: 이름 (기간)         ← Phase 정의
- [ ] 태스크 1                     ← 미완료 태스크
- [x] 태스크 2                     ← 완료 태스크
- 태스크 3                         ← 체크박스 없음 (태스크로 인식)
```

### 3.2 정규식 패턴

```python
# 섹션 헤더 탐지
SECTION_PATTERN = r'^##\s+(?:\d+\.\s+)?(?:구현\s*순서|Implementation\s*Order|구현\s*계획)'

# Phase 정의
PHASE_PATTERN = r'^###\s+Phase\s+(\d+-\d+):\s*(.+?)(?:\s*\(.*\))?$'

# 태스크 (체크리스트)
TASK_CHECKED_PATTERN = r'^-\s+\[x\]\s+(.+)$'
TASK_UNCHECKED_PATTERN = r'^-\s+\[\s*\]\s+(.+)$'
TASK_PLAIN_PATTERN = r'^-\s+(?!\[)(.+)$'  # 체크박스 없는 경우
```

### 3.3 파싱 알고리즘

```
1. 파일 전체 읽기
2. 섹션 헤더 탐지 (SECTION_PATTERN)
3. 섹션 내에서 Phase 블록 순회:
   a. Phase 헤더 매칭 (PHASE_PATTERN)
   b. Phase ID, 이름 추출
   c. 다음 Phase 헤더 또는 섹션 종료까지 태스크 수집
4. 의존성 추론:
   - Phase X-Y는 Phase X-(Y-1)에 의존
   - Phase X-1은 의존성 없음 (첫 Phase)
5. 구조화된 데이터 반환
```

### 3.4 파싱 예시

**입력**
```markdown
## 9. 구현 순서

### Phase 1-1: 기반 시스템 (1~2일)
- [ ] 프로젝트 설정
- [x] GameManager 싱글톤

### Phase 1-2: 플레이어 시스템 (2~3일)
- [ ] Player 씬/스크립트
- [ ] 터치 입력
```

**출력**
```yaml
phases:
  - id: "Phase 1-1"
    name: "기반 시스템"
    depends_on: []
    tasks:
      - name: "프로젝트 설정"
        status: "pending"
      - name: "GameManager 싱글톤"
        status: "completed"

  - id: "Phase 1-2"
    name: "플레이어 시스템"
    depends_on: ["Phase 1-1"]
    tasks:
      - name: "Player 씬/스크립트"
        status: "pending"
      - name: "터치 입력"
        status: "pending"
```

---

## 4. 상태 머신

### 4.1 Phase 상태 전이

```
                    ┌─────────────────────────────────┐
                    │                                 │
                    ▼                                 │
              ┌─────────┐                            │
   ┌─────────▶│ pending │◀───────────┐               │
   │          └────┬────┘            │               │
   │               │ start           │ pause         │
   │               ▼                 │               │
   │          ┌─────────────┐        │               │
   │          │ in_progress │────────┘               │
   │          └──────┬──────┘                        │
   │                 │ complete                      │
   │                 ▼                               │
   │          ┌───────────┐                          │
   │          │ completed │──────────────────────────┘
   │          └───────────┘     rollback
   │
   │ (선행 Phase rollback 시)
   │
   │          ┌─────────┐
   └──────────│ blocked │
              └─────────┘
```

### 4.2 상태 전이 규칙

| 현재 상태 | 이벤트 | 다음 상태 | 조건 |
|-----------|--------|-----------|------|
| `pending` | `start` | `in_progress` | 선행 Phase 모두 `completed` |
| `in_progress` | `complete` | `completed` | - |
| `in_progress` | `pause` | `pending` | - |
| `completed` | `rollback` | `pending` | - |
| `pending` | 선행 rollback | `blocked` | 선행 Phase가 `completed` 아님 |
| `blocked` | 선행 complete | `pending` | 선행 Phase 모두 `completed` |

### 4.3 Task 상태 전이

```
pending → in_progress → completed
   ▲                        │
   └────────────────────────┘
          (Phase rollback 시)
```

---

## 5. 의존성 관리

### 5.1 의존성 그래프 구조

```python
# 의존성 그래프 (방향: 선행 → 후행)
dependency_graph = {
    "Phase 1-1": [],
    "Phase 1-2": ["Phase 1-1"],
    "Phase 1-3": ["Phase 1-2"],
    "Phase 1-4": ["Phase 1-3"],
    "Phase 1-5": ["Phase 1-4"],
    "Phase 1-6": ["Phase 1-5"],
    "Phase 1-7": ["Phase 1-6"],
}

# 역방향 그래프 (후행 Phase 조회용)
reverse_graph = {
    "Phase 1-1": ["Phase 1-2"],
    "Phase 1-2": ["Phase 1-3"],
    "Phase 1-3": ["Phase 1-4"],
    # ...
}
```

### 5.2 의존성 검증 알고리즘

```python
def can_start_phase(phase_id: str, phases: dict) -> tuple[bool, list[str]]:
    """
    Phase 시작 가능 여부 검증

    Returns:
        (가능 여부, 차단 사유 목록)
    """
    phase = phases[phase_id]
    blockers = []

    for dep_id in phase.depends_on:
        dep_phase = phases[dep_id]
        if dep_phase.status != "completed":
            blockers.append(f"{dep_id}: {dep_phase.status}")

    return (len(blockers) == 0, blockers)
```

### 5.3 연쇄 영향 분석

```python
def get_affected_phases(phase_id: str, reverse_graph: dict) -> list[str]:
    """
    특정 Phase 변경 시 영향받는 모든 후행 Phase 조회
    (BFS로 순회)
    """
    affected = []
    queue = [phase_id]
    visited = set()

    while queue:
        current = queue.pop(0)
        if current in visited:
            continue
        visited.add(current)

        for dependent in reverse_graph.get(current, []):
            affected.append(dependent)
            queue.append(dependent)

    return affected
```

---

## 6. Serena Memory 연동

### 6.1 메모리 파일 구조

```
memories/
├── plan_meta.md          # 계획 메타정보 (변경 적음)
├── plan_progress.md      # 현재 진행 상태 (자주 변경)
└── plan_history.md       # 변경 이력 (append only)
```

### 6.2 plan_meta.md 형식

```markdown
# Plan Metadata

## Active Plan
- Name: Design_Phase1
- Source: docs/Design_Phase1.md
- Registered: 2025-01-15T10:00:00Z
- Revision: 1

## Phase Structure
| Phase | Name | Tasks | Depends On |
|-------|------|-------|------------|
| Phase 1-1 | 기반 시스템 | 4 | - |
| Phase 1-2 | 플레이어 시스템 | 5 | Phase 1-1 |
| Phase 1-3 | 적/탄환 시스템 | 4 | Phase 1-2 |
| Phase 1-4 | 웨이브 시스템 | 4 | Phase 1-3 |
| Phase 1-5 | 레벨업/어태치먼트 | 5 | Phase 1-4 |
| Phase 1-6 | 보스 | 4 | Phase 1-5 |
| Phase 1-7 | HUD/UI | 4 | Phase 1-6 |

## Statistics
- Total Phases: 7
- Total Tasks: 30
```

### 6.3 plan_progress.md 형식

```markdown
# Plan Progress

## Overview
- Plan: Design_Phase1
- Progress: 42% (3/7 Phases)
- Current Phase: Phase 1-3
- Last Updated: 2025-01-17T14:30:00Z

## Phase Status
| Phase | Status | Progress | Started | Completed |
|-------|--------|----------|---------|-----------|
| Phase 1-1 | ✅ completed | 4/4 | 2025-01-15 | 2025-01-15 |
| Phase 1-2 | ✅ completed | 5/5 | 2025-01-16 | 2025-01-16 |
| Phase 1-3 | 🔄 in_progress | 2/4 | 2025-01-17 | - |
| Phase 1-4 | ⏳ pending | 0/4 | - | - |
| Phase 1-5 | ⏳ pending | 0/5 | - | - |
| Phase 1-6 | ⏳ pending | 0/4 | - | - |
| Phase 1-7 | ⏳ pending | 0/4 | - | - |

## Current Task
- Phase: Phase 1-3
- Task: 적 이동/사격 패턴
- Status: in_progress
```

### 6.4 plan_history.md 형식

```markdown
# Plan History

## 2025-01-17
- 14:30 | phase_started | Phase 1-3
- 10:00 | phase_completed | Phase 1-2

## 2025-01-16
- 16:00 | task_completed | Phase 1-2 > 반격 시스템
- 14:00 | task_completed | Phase 1-2 > 에너지 흡수 메커니즘
- 09:00 | phase_started | Phase 1-2

## 2025-01-15
- 14:00 | phase_completed | Phase 1-1
- 10:30 | phase_started | Phase 1-1
- 10:00 | registered | Design_Phase1
```

### 6.5 동기화 전략

```
상태 변경 시:
1. .superclaud/progress.yaml 업데이트
2. Serena write_memory("plan_progress", ...) 호출
3. 이력 발생 시 write_memory("plan_history", ...) append

세션 시작 시:
1. list_memories() 호출
2. plan_progress.md 존재 시 읽기
3. .superclaud/progress.yaml과 비교
4. 불일치 시 최신 기준으로 동기화
```

---

## 7. 워크플로우 상세

### 7.1 계획 로드 워크플로우

```
/sc:plan-load <file>
        │
        ▼
┌───────────────────┐
│ 1. 파일 존재 확인  │
└─────────┬─────────┘
          │
          ▼
┌───────────────────┐
│ 2. 마크다운 파싱   │
│    - 섹션 탐지     │
│    - Phase 추출   │
│    - Task 추출    │
└─────────┬─────────┘
          │
          ▼
┌───────────────────┐
│ 3. 의존성 그래프   │
│    구축           │
└─────────┬─────────┘
          │
          ▼
┌───────────────────┐
│ 4. 중복 확인      │
│    (이미 등록?)   │
└─────────┬─────────┘
          │
          ▼
┌───────────────────┐
│ 5. progress.yaml  │
│    생성/업데이트   │
└─────────┬─────────┘
          │
          ▼
┌───────────────────┐
│ 6. Serena Memory  │
│    저장           │
└─────────┬─────────┘
          │
          ▼
┌───────────────────┐
│ 7. 결과 출력      │
└───────────────────┘
```

### 7.2 Phase 시작 워크플로우

```
/sc:plan-start <phase>
        │
        ▼
┌───────────────────┐
│ 1. Phase 존재 확인│
└─────────┬─────────┘
          │
          ▼
┌───────────────────┐
│ 2. 현재 상태 확인 │
│    (pending인가?) │
└─────────┬─────────┘
          │
          ▼
┌───────────────────┐
│ 3. 의존성 검증    │
│    (선행 완료?)   │──────No──────▶ 에러 출력
└─────────┬─────────┘
          │ Yes
          ▼
┌───────────────────┐
│ 4. 상태 변경      │
│    → in_progress │
└─────────┬─────────┘
          │
          ▼
┌───────────────────┐
│ 5. 파일 저장      │
│    + Memory 동기화│
└─────────┬─────────┘
          │
          ▼
┌───────────────────┐
│ 6. /sc:implement  │
│    자동 호출      │
└─────────┬─────────┘
          │
          ▼
┌───────────────────┐
│ 7. 결과 출력      │
└───────────────────┘
```

### 7.3 재설계 워크플로우

```
/sc:plan-redesign <phase>
        │
        ▼
┌───────────────────┐
│ 1. 영향 분석      │
│    (후행 Phase)   │
└─────────┬─────────┘
          │
          ▼
┌───────────────────┐
│ 2. 사용자 확인    │
│    - 사유 입력    │
│    - 영향 확인    │
└─────────┬─────────┘
          │
          ▼
┌───────────────────┐
│ 3. 원본 .md 수정  │
│    (사용자 편집)  │
└─────────┬─────────┘
          │
          ▼
┌───────────────────┐
│ 4. 재파싱         │
│    (변경사항 반영)│
└─────────┬─────────┘
          │
          ▼
┌───────────────────┐
│ 5. 영향 Phase     │
│    상태 처리      │
│    - 리셋         │
│    - 유지         │
└─────────┬─────────┘
          │
          ▼
┌───────────────────┐
│ 6. revision 증가  │
│    + 이력 기록    │
└─────────┬─────────┘
          │
          ▼
┌───────────────────┐
│ 7. 파일 저장      │
│    + Memory 동기화│
└───────────────────┘
```

### 7.4 자동 알림 로직

```python
def check_and_notify():
    """
    상태 변경 후 자동 알림 체크
    """
    current_phase = get_current_phase()

    if current_phase.status == "in_progress":
        # 모든 태스크 완료 체크
        all_completed = all(t.status == "completed" for t in current_phase.tasks)
        if all_completed:
            notify_completion_ready(current_phase)

    # 다음 Phase 시작 가능 체크
    next_phase = get_next_phase()
    if next_phase and can_start_phase(next_phase.id):
        notify_next_phase_ready(next_phase)
```

---

## 8. 에러 처리

### 8.1 에러 코드 체계

| 코드 | 분류 | 설명 |
|------|------|------|
| `E001` | 파일 | 파일을 찾을 수 없음 |
| `E002` | 파일 | 파일 읽기 실패 |
| `E003` | 파싱 | 섹션 헤더 없음 |
| `E004` | 파싱 | Phase 패턴 매칭 실패 |
| `E005` | 파싱 | 태스크 없는 Phase |
| `E010` | 상태 | 중복 계획 등록 |
| `E011` | 상태 | 계획 없음 |
| `E012` | 상태 | Phase 없음 |
| `E013` | 상태 | 잘못된 상태 전이 |
| `E020` | 의존성 | 선행 Phase 미완료 |
| `E021` | 의존성 | 순환 의존성 감지 |
| `E030` | 동기화 | Memory 쓰기 실패 |
| `E031` | 동기화 | YAML 파싱 실패 |

### 8.2 에러 메시지 템플릿

```python
ERROR_TEMPLATES = {
    "E001": "❌ 파일 없음: {file_path} 파일이 존재하지 않습니다.",
    "E003": "❌ 파싱 실패: '구현 순서' 섹션을 찾을 수 없습니다.\n"
            "   예상 형식: ## N. 구현 순서",
    "E004": "❌ 파싱 실패: Phase 패턴을 찾을 수 없습니다.\n"
            "   예상 형식: ### Phase X-Y: 이름",
    "E020": "⚠️ {phase_id}를 시작할 수 없습니다.\n"
            "   선행 Phase 미완료: {blockers}",
}
```

### 8.3 복구 가이드

```
E001 (파일 없음):
  💡 파일 경로를 확인하세요.
     현재 경로: {cwd}

E003 (섹션 없음):
  💡 계획서에 다음 형식의 섹션을 추가하세요:
     ## 9. 구현 순서
     또는
     ## Implementation Order

E020 (의존성 실패):
  💡 다음 명령으로 선행 Phase를 완료하세요:
     /sc:plan-complete {blocker_phase}
```

---

## 9. 구현 파일 구조

### 9.1 SuperClaude 프레임워크 측

```
SuperClaude/
├── commands/
│   └── plan/
│       ├── workflow.md              # 메인 라우터 정의
│       └── steps/
│           ├── step-load.md         # load 명령 처리
│           ├── step-start.md        # start 명령 처리
│           ├── step-complete.md     # complete 명령 처리
│           ├── step-status.md       # status 명령 처리
│           ├── step-next.md         # next 명령 처리
│           ├── step-tasks.md        # tasks 명령 처리
│           ├── step-deps.md         # deps 명령 처리
│           ├── step-history.md      # history 명령 처리
│           ├── step-rollback.md     # rollback 명령 처리
│           ├── step-redesign.md     # redesign 명령 처리
│           ├── step-impact.md       # impact 명령 처리
│           ├── step-list.md         # list 명령 처리
│           ├── step-switch.md       # switch 명령 처리
│           ├── step-unload.md       # unload 명령 처리
│           └── step-pause.md        # pause 명령 처리
│
├── modes/
│   └── MODE_Phase_Tracking.md       # Phase Tracking 모드 정의
│
├── data/
│   ├── PHASE_SCHEMA.yaml            # 상태 파일 스키마 정의
│   └── PLAN_PARSER_RULES.md         # 마크다운 파싱 규칙
│
└── RULES.md                         # Phase 추적 관련 규칙 추가
```

### 9.2 사용자 프로젝트 측

```
project/
├── .superclaud/
│   └── progress.yaml                # 상태 파일 (자동 생성)
│
├── docs/
│   └── Design_Phase1.md             # 원본 계획서
```

### 9.3 Serena Memory 측

```
memories/
├── plan_meta.md                     # 계획 메타정보
├── plan_progress.md                 # 현재 진행 상태
└── plan_history.md                  # 변경 이력
```

---

## 10. 확장 고려사항

### 10.1 향후 확장 가능 영역

| 영역 | 설명 | 우선순위 |
|------|------|----------|
| 다중 활성 계획 | 여러 계획 병렬 진행 지원 | 낮음 |
| 커스텀 의존성 | 사용자 정의 의존성 관계 | 중간 |
| Git 자동 연동 | 체크포인트 자동 커밋/태그 | 중간 |
| 시간 추정 | Phase별 예상 소요 시간 추적 | 낮음 |
| 리포트 생성 | 진행 현황 리포트 자동 생성 | 낮음 |
| 팀 협업 | 다중 사용자 상태 공유 | 낮음 |

### 10.2 확장 시 고려사항

```yaml
backward_compatibility:
  - 기존 progress.yaml 형식 유지
  - 새 필드는 optional로 추가
  - 버전 필드로 마이그레이션 지원

performance:
  - 대용량 계획서 파싱 최적화 (100+ Phases)
  - Memory 동기화 배치 처리
  - 캐싱 전략

security:
  - 상태 파일 무결성 검증
  - 원본 파일 수정 시 백업
```

---

## 다음 단계

- **개요 및 사용법**: [PHASE_TRACKING_OVERVIEW.md](./PHASE_TRACKING_OVERVIEW.md)
- **명령어 레퍼런스**: [PHASE_TRACKING_COMMANDS.md](./PHASE_TRACKING_COMMANDS.md)
